# 浑晶前端 env 模板
# 复制为 .env.production(生产)或 .env.development(开发)

# === API 后端地址 ===
# 开发:留空(用 vite proxy 走 localhost:8000)
# 生产:留空(用 nginx 同源代理 /api/* → 后端 8000)
VITE_API_BASE=

# === 上线监控:Sentry(2026-06-02 加)===
# 注册 https://sentry.io 拿前端项目的 DSN(跟后端是两个独立项目)
# 详细教程:docs/sentry_setup.md
# 本地开发不填 → 跳过初始化,不影响.
# 生产填上 → 前端自动捕获 JS uncaught error + 路由错误 + 慢请求
VITE_SENTRY_DSN=

# === 洞察后台埋点地址(2026-06-25 加)===
# 开发:留空(useAnalytics 回退 localhost:8001)
# 生产:必须填 insights 子域,否则线上埋点会向 localhost:8001 发 HTTP 请求 →
#       被浏览器 mixed-content 拦死 + 静默吞异常 = 线上行为数据 100% 丢失。
# 部署洞察后台后改这里并【重新 build 主前端 dist】才生效(Vite 把它静态内联进 bundle)。
VITE_INSIGHTS_BASE=https://insights.shuangdayeye.cn
