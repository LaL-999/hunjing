# 浑晶前端 env 模板
# 复制为 .env.production(生产)或 .env.development(开发)

# === API 后端地址 ===
# 开发:留空(用 vite proxy 走 localhost:8000)
# 生产:留空(用 nginx 同源代理 /api/* → 后端 8000)
VITE_API_BASE=

# === 上线监控:Sentry(2026-06-02 加)===
# 注册 https://sentry.io 拿前端项目的 DSN(跟后端是两个独立项目)
# 详细教程:docs/sentry_setup.md
# 本地开发不填 → 跳过初始化,不影响.
# 生产填上 → 前端自动捕获 JS uncaught error + 路由错误 + 慢请求
VITE_SENTRY_DSN=
