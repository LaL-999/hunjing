# 生产构建环境变量 —— `npm run build`(production 模式)自动加载,dev 不受影响。
#
# 为什么 Web 也要显式指 api 子域:
#   Web 前端跑在 app.shuangdayeye.cn,但该子域的 nginx 只服务静态文件、**没有反代 /api**
#   到后端(只有 api.shuangdayeye.cn 那块反代)。若用相对 /api,GET 会落到 SPA 返 HTML、
#   POST 会落到静态文件返 405(登录发不出验证码即此根因)。所以直接打 api 子域,
#   跨域由后端 CORS 放行 app 子域解决(与桌面客户端同一路子,已验证可用)。
VITE_API_BASE=https://api.shuangdayeye.cn

# 埋点事件发往洞察后台子域(否则回退 localhost:8001,线上 100% 丢)。
VITE_INSIGHTS_BASE=https://insights.shuangdayeye.cn
