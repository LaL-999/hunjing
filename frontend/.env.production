# 生产构建(npm run build)—— 默认面向 Web 端(app.shuangdayeye.cn)。
#
# Web 用【同源相对 /api】:app 站 nginx 已配 /api 反代到后端,所以不设 VITE_API_BASE,
# 走同源 → 零跨域、零 CORS 预检、复用页面连接 → 最快(实测后端经 nginx 仅 ~100ms,
# 跨域版被预检+独立建连拖到 400ms~数秒,同源版直接贴近后端真实速度)。
#
# ⚠️ 桌面客户端(Tauri,origin = tauri://localhost)没有同源 /api,
#    构建桌面包时【必须】显式传 VITE_API_BASE=https://api.shuangdayeye.cn
#    (在 desktop release CI 的 env 里设),否则桌面端连不上后端。详见 DESKTOP_CLIENT.md。
VITE_INSIGHTS_BASE=https://insights.shuangdayeye.cn
