# 剧创态 · 参赛构建蓝图

> **赛题**:七牛云暑期实训营 · 题目三「AI 小说转剧本工具」
> **形态**:作为 **浑晶** 平台的新增「创作态」子功能(暂定名 **剧创态**,与 漫创态 同构命名)
> **执行者**:Claude Code(深度结合 浑晶 现有代码库实现)
> **本文档作用**:`做什么 / 为什么 / 按什么顺序 / 如何对齐评分`。平台特定的接线细节由 Claude Code 读代码后自行决定。

---

## 0. 给 Claude Code 的第一条指令

在写任何代码之前,**先通读 浑晶 现有代码库**,至少弄清楚四件事,并把结论写进一份 `INTEGRATION_NOTES.md`:

1. **前后端技术栈与目录结构**(本功能要复用同一套,不要另起炉灶)。
2. **「中间态」是如何从已有小说抽取人物 / 关系 / 事件的**——这套抽取结果就是本功能要复用的「故事圣经」。确认它的数据结构、调用方式、是否可被新模块直接读取。
3. **多 Agent 编排是怎么组织的**(本功能的转换流水线优先复用同一套编排框架)。
4. **模型调用层**:确认 DeepSeek V4 Flash 的接入方式(API 封装、超时/重试、结构化输出的处理)。

读完后,再用本蓝图去落地。**凡是复用 浑晶 已有能力的地方,都要在对应 PR 描述里注明「复用平台 XX 模块」**(这是合规硬要求,见 §8)。

---

## 1. 战略定位:这场比赛到底在评什么

这不是普通 hackathon,而是「用项目代替笔试」的**招聘漏斗**。三项评分里:

- **作品完整度与创新性 40%**:产品设计、功能完整、交互流畅、是否新颖。
- **开发过程与质量 40%**:架构、代码健壮性、**PR 数量与质量、commit 分布合理性**。
- **演示与表达 20%**:demo 视频是否讲清楚。

**关键洞察**:那 40% 的「过程分」是绝大多数选手会忽视、却最容易拉开差距的地方。他们在评「你是不是一个值得收的工程师」。所以本蓝图的核心策略是:**让真实的模块演进顺序,天然等于一份漂亮的 PR 计划**(见 §4 与 §8),做到「好架构」和「好 git 历史」是同一个动作。

**单人参赛的取舍**:没有分工压力,但 commit 必须均匀铺满三天。绝不能出现「前两天空、最后一天一次性导入」——规则明确这会直接判无效。本蓝图的三天节奏(§9)已据此设计。

---

## 2. 合规风险与仓库策略 `决策点`

**风险**:复用已有平台代码 ⨯ 规则「所有 commit 时间戳必须在窗口内」「复用自己过去的代码片段未在 PR 注明来源 = 无效作品」。组织方批准「可作为子功能,README 说明清楚即可」,但这**不等于**批准把 浑晶 旧代码当参赛作品提交。

**推荐策略(需 Joyce 确认)**:

1. **新建一个独立的公开仓库**,开题后创建,窗口内全部为全新提交。
2. 浑晶 作为「父平台 / 外部依赖」在 README 中声明其背景与能力,**不把平台旧代码搬进参赛仓库**。
3. 剧创态 这一层(摄入 → 场景切分 → 转换流水线 → Schema → 编辑器 UI → 精修)本身就是足量的全新工作,既合规、commit 历史又自然漂亮。
4. 若功能上必须调用平台能力(如 中间态 的抽取),**优先通过接口 / 服务调用**,而非复制代码;确需引用的少量代码,在 PR 描述与 README 同时注明来源。

> **决策点 D1**:是否接受「新建独立公开仓库 + 浑晶 作平台依赖声明」这一边界?(强烈推荐接受)

---

## 3. 产品形态:剧创态

把本功能做成 浑晶 的一个新「创作态」,沿用平台导航与设计语言,入口与 初始态 / 中间态 / 末尾态 / 漫创态 并列。

**与现有「态」的关系**:

- **复用 中间态**:中间态 已能从已有作品拆解人物/关系/事件 → 这就是「故事圣经」。剧创态 直接消费它,作为转换时的一致性骨架。这是本功能质量的命脉,也是你的最大护城河。`决策点 D2:确认复用 中间态 的抽取结果`
- 剧创态 的独有职责:**场景切分 + 小说语法 → 剧本语法 + 结构化 YAML + 剧本编辑器**。

**核心用户流程**:
导入小说(≥3 章)→ 复用/触发故事圣经抽取 → 自动场景切分 → 逐场转换 → 生成结构化剧本 YAML → 在编辑器里查看 / 改 / 单场重生成 → 导出。

---

## 4. 架构:转换流水线(核心逻辑)

**反对一锤子**:不要把三章丢给模型一句「转成剧本 YAML」。那样场景乱断、内心戏丢失、人物名漂移、格式不稳。**改用分阶段流水线**,每阶段职责单一、可单独测试、对应一个 PR:

| 阶段 | 职责 | 复用 / 新建 |
|---|---|---|
| 1. 摄入 | 导入/粘贴、按章节解析、分块、归一化 | 新建 |
| 2. 故事圣经 | 人物/地点/关系/事件/时间线 | **复用 中间态** |
| 3. 场景切分 | 按「地点 × 时间 × 事件」跃迁判定 scene 边界 | 新建 |
| 4. 逐场转换 | prose → 剧本元素(场景头/动作/对白/旁白) | 新建,核心 |
| 5. 组装 + 校验 | 拼 YAML、按 Schema 校验、失败重试 | 新建 |
| 6. 精修回路 | 用户改某场 / 单场重生成 | 新建 |

**针对 DeepSeek V4 Flash 的设计考量**:

- **长上下文**:Flash 支持长文本,故事圣经可整体作为上下文喂入,保证跨场一致性。
- **速度快但创意上限不如 Pro**:正因如此,**越要靠流水线把"难任务"拆成"小而明确的子任务"**——场景切分、对白归属、内心戏转译分开做,各自给精准 prompt,模型更稳。这也是写进 SCHEMA_DESIGN 与 README 的一个有力工程论证。
- **YAML 可靠性**:模型直出 YAML 偶有格式错。在 §5 校验阶段做**严格校验 + 自动重试 + 局部修复**,而不是信任一次生成。

---

## 5. YAML Schema 设计(主交付物之一)

题目特意要求「额外写一篇文档说明 Schema 设计原因」——这是强信号:**他们评的不是你会写 YAML,而是你为什么这样设计**。Claude Code 必须产出 `SCHEMA_DESIGN.md`,逐条讲清取舍。

**设计原则**(每条都要在文档里给出"为什么"):

- **稳定 ID**:每个 scene / element 有不变 id → 支持「改完某场再局部重生成」的往返编辑。
- **元素分型**:`action / dialogue / parenthetical / transition`,外加难点项 `voiceover / flashback_start / flashback_end` → **把"内心戏怎么落地"显式编码进 Schema**。
- **结构与内容分离**:场景头(内/外景、地点、日/夜)独立于正文。
- **可溯源**:每个场景挂 `source`(原文章节/段落)→ 作者敢信任产物,也展示工程心思。
- **保真度标记**:`fidelity` 字段标注转换有损的场景,提醒作者复核。
- **校验友好**:可配套一份 JSON Schema 做机器校验。
- **为什么 YAML 而非 JSON**:人类可读、可手改、能写注释,适合"待打磨的草稿"。文档里参照 **Fountain / Final Draft(FDX)** 等既有标准,再说明为什么走到"结构化 YAML"(可机读、可查询、能挂故事圣经)。

**Schema 骨架(给 Claude Code 的起点,可演进)**:

```yaml
meta:
  title: "剧名"
  source: "原小说标题"
  logline: "一句话梗概"
  adapted_from_chapters: [1, 2, 3]
  generated_by: "浑晶 · 剧创态"
  schema_version: "1.0"

characters:
  - id: char_001
    name: "林深"
    aka: ["林先生"]
    description: "三十岁,沉默寡言的钟表匠"
    first_appearance: scene_001

locations:
  - id: loc_001
    name: "老城钟表铺"
    int_ext: INT          # INT | EXT | INT/EXT
    description: "堆满旧钟的逼仄店面"

scenes:
  - id: scene_001
    number: 1
    heading:
      int_ext: INT
      location_id: loc_001
      time_of_day: 日       # 日 | 夜 | 黄昏 | 黎明 | 连续
    summary: "林深修表时,陌生女子推门而入。"
    characters: [char_001, char_002]
    source:                 # 可溯源
      chapter: 1
      paragraph_range: [3, 9]
    fidelity:               # 保真度标记
      level: high           # high | medium | low
      notes: "原文含大段内心独白,已转为动作外化"
    elements:
      - type: action
        id: el_0001
        text: "林深低头打磨齿轮,门铃响。他没抬头。"
      - type: dialogue
        id: el_0002
        character_id: char_002
        parenthetical: "(声音发抖)"
        text: "你能修好它吗?"
      - type: voiceover
        id: el_0003
        character_id: char_001
        text: "我一眼认出那是父亲的怀表。"
        note: "由内心独白转化,作者可删除或改为动作外化"
```

---

## 6. 内部逻辑:怎么得到高质量产物

这道题的灵魂不在"转换",在"**改编**"。小说与剧本是两种语法,以下四个转换难点决定产物质量,要在逐场 prompt 中各个击破:

1. **场景切分**:小说时空流动,无干净 scene break。按地点/时间/事件跃迁判定边界。
2. **内 → 外**:剧本不能直接演"她感到恐惧""他想起童年"。必须转成可见动作、对白,或显式 V.O./闪回(编码进 Schema 的 `voiceover`/`flashback_*`)。
3. **铺垫与说明**:夹叙夹议的背景 → 变对白 / 视觉叙事 / 删除。
4. **对白抽取与归属**:从叙述里剥离对白,正确归到说话人(代词消解、"他说"与零标注对白的区分)——这里用故事圣经的人物表辅助归属,准确率显著提升。

**质量保障机制**:

- **故事圣经作一致性骨架**:每场转换都把相关人物/地点喂进去,杜绝名字漂移、人物失踪。
- **逐场而非整体**:把难任务切小,Flash 模型更稳。
- **校验回路**:Schema 校验 + 失败重试 + 局部修复。
- **保真度标记**:内心戏密集/说明文字多的场景标黄,提醒复核。
- **可溯源**:原文 ↔ 剧本对照,建立信任。
- **(加分项)改编决策摊给作者**:遇到内心独白,不偷偷决定,而是给选项(转 V.O.?动作外化?删除?)让作者选。新颖且呼应"尊重作者创作主权"。`决策点 D3:时间允许才做`

---

## 7. 界面:剧创态 UI

沿用 浑晶 设计语言(奶白底、紫色点缀、克制文气、字体驱动),做成一个"写作工具"而非花哨 dashboard。

**核心布局**:

- **双栏对照**:左原文 / 右剧本,溯源高亮(点某场景高亮其原文出处)。
- **故事圣经侧栏**:摊出人物、地点(复用 中间态 数据),秀分析深度。
- **剧本渲染视图** + **原始 YAML 切换**:渲染视图用 fountain-like 排版(场景头大写、对白居中、动作左对齐),让产物"像剧本"。
- **逐场编辑 / 单场重生成**按钮。

**范围警告**:**界面不能吃掉 72 小时的预算**。一个聚焦精致的工具,永远赢过一个铺得很大但处处半成品的东西。UI 在 Day 3 集中做。

---

## 8. 过程分打法:PR / commit / README / demo

这部分直接对应 40% 过程分 + 20% 演示分,且决定是否触发"无效作品"。**务必严格执行**。

### PR 规范(每个 PR 必须做到)

- **每个 PR 只做一件事**,粒度尽量小,大功能拆多个独立 PR。
- **基于 PR 添加功能**(不要直接推主分支)。
- **PR 描述四件套**(缺一不可):
  - 标题:一句话说明本 PR 新增/修改了什么
  - 功能描述:该功能的作用与使用方式
  - 实现思路:技术选型或核心逻辑
  - 测试方式:如何验证功能正常
- **复用 浑晶 能力的 PR,描述里注明「复用平台 XX 模块」**(合规硬要求)。
- **PR 合并后主分支必须可跑**,评委随时能复现演示。

### commit 分布

- 三天均匀提交,每天都有有意义的 commit。**禁止临尾突击**。
- 所有 commit 时间戳落在窗口内。

### README 必须包含

1. 项目简介 + **明确声明:本项目是 浑晶 平台的「剧创态」子功能**。
2. 父平台 浑晶 背景(独立开发)+ **本次比赛的范围边界**:哪些是窗口内全新开发、哪些是复用平台能力。
3. **技术栈与依赖清单**:DeepSeek V4 Flash、前后端框架、关键库——规则要求第三方依赖必须列明,否则无效。
4. **复用声明**:复用了平台的哪些模块(与 PR 注明一致)。
5. 运行/部署说明(`/frontend` 与 `/backend` 分目录)。
6. YAML Schema 说明,链接到 `SCHEMA_DESIGN.md`。
7. **demo 视频链接**(bilibili/云盘等外部可访问,可播放)。
8. 如何复现演示效果。

### demo 视频脚本要点(20% 分)

- **人声讲解**,完整覆盖核心模块:摄入 → 故事圣经 → 切分 → 转换 → YAML 产物 → 编辑/重生成。
- 重点展示**改编质量**:内心戏怎么处理、跨场一致性、可溯源——这是评委最容易被打动的点。
- 3–5 分钟,结构清晰,先讲它是 浑晶 的子功能。

---

## 9. 三天执行节奏

全程主分支保持可跑。下表是 PR 与 commit 锚点。

**Day 1 — 脚手架 / Schema / 摄入 / 接通故事圣经**
- PR#1 剧创态 模块脚手架(路由、前后端目录、接入平台导航)
- PR#2 定义 YAML Schema + 产出 `SCHEMA_DESIGN.md`(先文档后代码)
- PR#3 小说摄入与章节解析(导入/粘贴、分块、归一化)
- PR#4 接入 中间态 的人物/关系/事件抽取作为故事圣经(**注明复用**)

**Day 2 — 流水线核心**
- PR#5 场景切分(地点/时间/事件跃迁)
- PR#6 逐场转换:动作抽取(prose → 可见动作)
- PR#7 逐场转换:对白抽取与归属
- PR#8 内心戏/铺垫转译(V.O. / 动作外化 / 闪回)
- PR#9 组装为 YAML + Schema 校验 + 失败重试

**Day 3 — 精修 / UI / demo**
- PR#10 剧本渲染视图(fountain-like)+ 原始 YAML 切换
- PR#11 逐场编辑与单场重生成
- PR#12 保真度标记 + 原文↔剧本溯源高亮
- PR#13(加分)改编决策选项交互
- PR#14 README 完善 + 主分支冒烟测试 + demo 视频录制/上链接

---

## 10. 给 Claude Code 的交付物清单

- [ ] `INTEGRATION_NOTES.md`(读完代码库后的整合结论,见 §0)
- [ ] 剧创态 完整可运行代码(`/frontend` + `/backend`)
- [ ] `SCHEMA_DESIGN.md`(逐条讲清 Schema 设计原因)
- [ ] 配套 JSON Schema(机器校验,可选但加分)
- [ ] `README.md`(含 §8 全部必含项)
- [ ] demo 视频脚本(供 Joyce 录制)
- **必守约束**:commit 在窗口内 / PR 四件套 / 复用必注明 / 主分支始终可跑

---

## 11. 决策点汇总(给 Joyce)

- **D1 仓库策略**:新建独立公开仓库 + 浑晶 作平台依赖声明 —— 是否接受?(推荐:接受)
- **D2 复用范围**:剧创态 是否复用 中间态 的人物/关系/事件抽取作故事圣经?(强烈推荐:复用)
- **D3 加分项**:「改编决策摊给作者」是否做?(时间允许才做,MVP 不依赖它)
- **D4 命名**:子功能定名「剧创态」?(与 漫创态 同构)
- **D5 部署**:评委需可复现 —— 仅本地可跑 + README 清晰即可,还是另公开部署一个 demo 站点?(建议:至少本地可跑 + README 详尽;有余力再部署)
