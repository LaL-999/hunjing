# 项目记忆 v15.1(精简工作版)

最后更新:2026-05-22 — **本文件是工作记忆,保留当前活跃状态、铁律、商业模型、产品形态、24 条开发避坑、文档索引。** 已完工 sprint 详情(M1-M9 / MP / CT / TS / INIT / D.7 / D.8 / D.9 / Sprint 5 等约 3000 行实施叙事)归档到 **[docs/history/项目记忆_v15.1_full_snapshot.md](docs/history/项目记忆_v15.1_full_snapshot.md)** —— 需要某个 sprint 完整原文时去那里查。

---

## 🆕 Sprint 6.A2 FOCUS.2(2026-05-22):作品叙述视角识别 + 续写一致性

**用户痛点**:之前的 prompt 改造只处理了角色归一(《挪威的森林》"我" vs "渡边"分裂),但 **AI 续写时的"人称漂移"问题未治** — 长续写到中后段 AI 容易把第一人称改写成第三人称,跨代续作时跨代失忆人称。**为打造"高标准、高质量平台"严肃做了全栈方案。**

**完整 6 层全栈**:

| 层 | 文件 | 改动 |
|---|---|---|
| ① 数据 | `migration 055_projects_narrative_pov.sql` + `models/project.py` + `schemas/project.py` + `routers/projects.py` | projects 表加 `narrative_pov TEXT` 列;Project 模型加字段 + from_row 兜底;Pydantic Literal 拦截非法枚举;PATCH 端点处理空字符串 → null |
| ② 抽取 | `prompts/build_graph.md` v4→v5 + `services/llm_extract.py::_merge_graphs` + `services/extract_service.py` | LLM 输出加 `meta.narrative_pov`(4 枚举判定规则);多 chunk 投票聚合(>60% majority / 不一致 → mixed);写回 projects.narrative_pov |
| ③ 续写 | `prompts/m3_narrator.md`(铁律 9)+ `prompts/m6_outline_generator.md`(铁律 11)+ `services/agent_evolution_engine.py::_narrator_compose` + `services/outline_generator.py` | 各续写 prompt 加铁律"严格延续 narrative_pov";服务层从 projects 读取并注入 user_input |
| ④ 自检 | `services/consistency_checker.py` | `_strip_dialogue_quotes()` 剥离对话 + `_check_narrative_pov_drift()` 纯程序检测;check_segment 入口跑(零 LLM 成本);漂移 → critical 违规触发 M4.3 retry 回路 |
| ⑤ 前端 | `api/types.ts` + `views/ProjectView.vue` | `NarrativePov` 类型 + `Project.narrative_pov`;header native select chip(未识别灰 / 4 枚举紫);onPovChange PATCH |
| ⑥ 测试 | `tests/test_narrative_pov.py` | 13 测试:PATCH + 非法枚举 + 漂移检测 6 case + 多 chunk 投票聚合 4 case |

**枚举**:`first` / `second` / `third` / `mixed` / `null`(未识别)

**程序漂移检测阈值**:
- first 模式:每 500 字至少 1 次"我"(否则 critical)
- third 模式:叙述层"我" ≥ 3 次(critical)
- second 模式:每 500 字至少 1 次"你"
- mixed / null:跳过

**对话剥离**:中文双引号 `"..."` + 直角引号 `「...」` + 英文双引号 全部正则剥离,只剩叙述层做统计。

**4 重保险闭环**:
```
① 抽取 → LLM 识别人称写到 project.narrative_pov(各 chunk 投票)
② 续写 → 3 个 prompt 看到 narrative_pov,强制延续
③ 自检 → 程序级扫人称代词,漂移触发 critical
④ 重写 → 复用 M4.3 narrator retry 回路,LLM 改正人称
```

测试:13/13 ✅(本任务)+ 回归 67/67 ✅(narrative_pov + projects + consistency_checker + extract + agent_evolution_engine)+ vue-tsc 0 错。

---

## 🆕 Sprint 6.A2 FOCUS(2026-05-21):AI 角色对焦扩展 + 角色合并兜底 + 抽取归一治根

3 个子任务,治"AI 抽角色时同一人分裂"+"AI 对焦看不到 INIT.3 行为基线"+"两张卡需要手动合并"。

### A. 抽取 prompt 角色归一(治根)

`prompts/build_graph.md` v3→v4,在约束 #1 后插入 **铁律 1.A 角色归一**:

- (a) **第一人称代词归一**:文本第一人称叙述时,"我" 必须折叠到主角的 aliases,不能单开 PERSON(治《挪威的森林》"我" vs "渡边"分裂)
- (b) **职业 / 身份称谓归一**:"驹子" = "艺妓" = "五等艺妓" → 合并为 1 PERSON(治《雪国》分裂)
- (c) **关系称谓归一**:"母亲" + "李夫人" 是同一人时 → 合并,关系称谓进 aliases

**输出自检 4 步 checklist**,LLM 输出 JSON 前必跑。

### B. AI 角色对焦扩展到 behavior_baseline 4 维

`prompts/character_focus.md` 类型 5→7,加 `behavior_baseline_补充`;`services/refine_service.py` `VALID_KINDS` 加新 kind;`migration 053` 扩 character_refinements.suggestion_kind CHECK;前端 types.ts 扩 `SuggestionKind`;`CharacterFocusModal.vue` KIND_LABEL 加新条目;6 个测试。

### C. 用户手动合并角色卡片(给 AI 抽取归一错误兜底)

对齐 `project_scenes` 合并语义:`migration 054_characters_aliases.sql`;`models/character.py` `aliases` 字段 + from_row;`services/extract_service.py` INSERT 时持久化 aliases;新 `services/character_merger.py::merge_characters()`(aliases 累积+15 上限 + relationships reassign + events.participants 替换 + canonical_entities/action_ledger/emotional_state_chain/character_arcs/foreshadow_ledger 级联);`routers/characters.py` `POST /projects/:id/characters/merge`(409 MERGE_SELF / 404);7 个测试;前端 `ProtagonistWall.vue` 卡片"⋯"菜单加"⊕ 合并到其他角色"(source 黄色 / target 紫色虚线 + 整张卡 onCardClick handler)。

### D. 主角阈值调整(用户痛点"15 个主角太多")

`services/protagonist_judger.py` 加 `MIN_PROTAGONIST_SCORE = 0.40`;`_apply_protagonist_cap` 双过滤(is_candidate + score ≥ 0.40);应用效果雪国 15→4-7 主角。

---

## 🆕 Sprint 6.A2 UI 收尾 + bug 修复(2026-05-21,12 改动)

### UI 优化

1. **场景图谱合并按钮初始态隐藏** — `SceneGraph.vue` 加 `v-if="!allowCreate"`
2. **chip 内 emoji 全部 SVG 化** — `Icon.vue` 加 `filled` prop + sparkles icon;strokeWidth 默认 2 → 1.5;ProjectView 所有 chip 替换为 SVG
3. **5 个 SVG section header** — character / message / settings / world / scene / event

### Bug 修复

1. **事件参与者 chip 点击退出 edit** — `<label>` + checkbox 改为 `<button>` + `@mousedown.prevent`
2. **关系编辑两端同步进 edit** — `RelationshipRow` 加 `editingFromCharId` + 模板 v-if 校验
3. **SceneGraph 创建后场景列表不显** — `v-else` 改独立 v-if;empty state 限非 allowCreate
4. **场景图谱 toast"再次点击「破屋」取消"自打开** — 修复 ⊕ 按钮 toggle 逻辑

---

## 🆕 v15.0/15.1 关键工程债清理(2026-05-22)

### A1 ✅ build_graph v5/v6 verify + FOCUS.3 实战驱动迭代

**v5 LOCKED**(初版,4/4 PASS,¥0.0243)— 场景 A/B/C/D 全过

**v5 实战暴露 2 个 bug**(用户重抽《挪威的森林》):
- bug 1:"小林绿子" vs "绿子" 分裂 — v4 铁律 (a)(b)(c) 漏了"全名↔短称"
- bug 2:narrative_pov 训练偏见 — LLM 看到"渡边"联想《挪威的森林》原作判 first

**v6 LOCKED**(2026-05-22,5/5 PASS,¥0.0351):
- prompt 加铁律 (d):全名↔短称归一(name 取频次更高的,另一个进 aliases)
- prompt 加 narrative_pov 反训练偏见铁律:只数当前输入 `{text}` 叙述层代词频次
- 自检 checklist 加"子串扫描"
- 场景 E(全名归一,陌生角色名"沈墨竹/墨竹") + verdict 逻辑修正
- 报告:`data/prompt_verifications/build_graph_v5_20260522_002513/`
- 新工具 `scripts/verify_build_graph.py` 后续可复用

### A4 ⏸ character_focus verify 仍未做(性价比低,留待实战驱动)

- v3 + behavior_baseline_补充 没单独 verify
- character_focus 的 LLM 行为已被 v1→v3 三轮 verify(2026-05-09)证实稳定
- 工程债保留,等用户实测发现具体失效 case 再 verify(¥0.05-0.10)

### A2 ✅ 全量 pytest 验证(2026-05-22 6m51s)

- 跳过 test_comic_service(PIL/Pillow 未装的假阳性)
- **505/505 PASS,0 退化**
- 25 warning 全是 starlette/fastapi `HTTP_422_UNPROCESSABLE_ENTITY` deprecation,与改动无关
- vue-tsc 0 错

### Migration 累积(本周)

| migration | 用途 |
|---|---|
| 052 | events.time_anchor(INIT.7) |
| 053 | character_refinements.suggestion_kind CHECK 扩展(加 evolution_hint + behavior_baseline_补充) |
| 054 | characters.aliases_json |
| 055 | projects.narrative_pov |

---

## ⚠️ 过期/已废弃概念索引(读历史前先看这个)

新读者注意:历史中以下概念**已废弃 / 已重命名**,见到时按新概念理解,不要按历史规划开发:

| 历史概念/规划 | 现状 | 应理解为 |
|---|---|---|
| **"我的剧情线"**(DashboardView 全屏推演列表)| 已废弃(Sprint 6.A2 M7.D)| 改为"作品列表"(项目页 Tab) |
| **Sprint 5 漫画态质量大改 10 子项** | 已废弃(漫创态降级)| 漫创态进"实验中"长期搁置,等行业生图模型质变 |
| **OASIS fork 方案** | 已废弃(很早) | 自建 FastAPI + SQLite,无依赖 |
| **"分类用户"模型**(只续写不创作) | 已废弃 | 4 创作态 + 漫创态独立 |
| **AI 重塑按钮 / AI 续写按钮文案** | 多轮迭代 | 按当前 `simulateButtonLabel` 计算逻辑(按 mode 派生) |
| **"禁忌(绝不做)"** | INIT.8 改 | "倾向避免(尽量别)" — 非硬约束,剧情合理可破 |
| **"15 个主角太多"** | FOCUS.2(2026-05-22)加阈值 0.40 | MIN_PROTAGONIST_SCORE = 0.40,双过滤 |
| **AI 抽角色分裂** | FOCUS(2026-05-21)build_graph v3→v6 加角色归一铁律 | 用 aliases 累积;LLM 仍漂移时用 ⊕ 合并按钮兜底 |
| **AI 续写人称漂移** | FOCUS.2(2026-05-22)全栈方案 | project.narrative_pov + 续写 prompt 铁律 + 程序级漂移检测 + retry 回路 |
| **真支付通道**(Stripe/微信/支付宝 webhook) | 推迟到 E 部署阶段 | 目前 addon/purchase mock 立即发 credit,不要现在做支付集成 |
| **comic_batches 表** | 保留未用 | orchestrator 内存循环就够 |
| **次数 quota(comics_per_month 等)** | Sprint C.1 重构为 credit 单位制 | 见商业模型章节;不要再做次数 quota |

---

## 📜 已完工 Sprint Milestone 速查(详情见 snapshot)

下面是项目从 B 阶段以来所有已完工的关键 sprint,每条一行。**完整实施细节、bug 修复历程、测试矩阵**在 `docs/history/项目记忆_v15.1_full_snapshot.md` 对应章节里。

### Sprint 6.A2(2026-05-18 → 2026-05-22)灵魂续写主线
- **M1 关系时间轴 + 演化提示** — relationship_phases 表 + 用户改 type/strength 后 director 自动拉新值
- **M2 场景图谱 + 角色亲疏地图** — scene_extractor / character_affinity / SceneGraph UI
- **M3 灵魂续写主路径**(A/B/C 三阶段)— agent_runtime + agent_evolution_engine + m3_scene_picker / m3_agent_dialogue / m3_narrator prompt + RAG 召回 + 模式开关 ⚡快速 vs 🎭灵魂
- **M3.D fix1-4** — 字数失控修复(reshape_to_recommended_chars 推荐区间)
- **M4.1 全局事实账本 + 主线追踪** — world_facts + plot_threads 表 + plot_tracker / world_state_extractor
- **M4.2 round 内同步 + 套语去重** — agent dialogue 之间硬约束 + phrase_blacklist
- **M4.3 Reflexion 一致性自检 + 角色行为基线** — consistency_checker + behavior_baseline 4 维度
- **M5 治本工程** — canonical_entities / action_ledger / emotional_states / temporal_lock / hard_constraints + entity_registrar / action_extractor / emotional_state_tracker
- **M6 Outline-First 长篇生成** — outline_generator + outline_orchestrator + OutlineReviewView(加幕/删幕/拖拽重排,M8.B 完工)
- **M7.A-H** 正典守护者新角色豁免 / 续作新角色入库 / 自洽守护者「采纳」/ IA 项目总览 / IA 再修 / 同项目单作品拦截 / no_go_list 抽取强度降低 / 关系类型自由化 + hash 派生色
- **M8.A-D** 场景动态扩展入库 / outline 编辑增强 / 场景管理面板 / 场景多样性健康度 chip
- **M9.A 长篇心智(滚雪球深化)** — character_arcs / foreshadow_ledger / world_rules_ledger + chain_context_builder + long_form_mind_extractor
- **M9.B Outline 与反事实冲突优先级** — director_system.md 加铁律 9.6
- **MP Planner 介入 + 张力调节**(灵魂续写最后一环)
- **CT 反事实组合树**(创作者层付费杠杆)+ **CT-OPT** 并发预估修正 + 429 限流退避
- **TS 末尾态笔法 Reflexion 自检**(北极星强化)— tail_style_analyzer / tail_style_checker
- **INIT.1-11** 初始态前置条件富化:关系移入角色卡 + 关系时间轴 + 行为基线 4 维 + 主角钉死 chip + 世界观 6 维编辑 + 场景预定义 + 事件时间锚 + no_go_list 语义柔化 + 台词风格 UI 强化
- **FOCUS / FOCUS.2 / FOCUS.3** 抽取归一治根 / narrative_pov 全栈 / build_graph v6

### Sprint 6.A1
- 多 agent 仿真奠基 — 主角判定 + agent 档案补全

### Sprint 5(漫画态质量)— 整段已废弃
- 5.1 并行生图 / 5.4 画风 v3 / 5.7 镜头 prompt / 5.8 角色锚定 / 5.9 漫画气泡 / 5.10 工程层强制兜底 / 5.10+ retry_style_candidates / 5.11 Reflexion 视觉自检 — **全部归档**,漫创态进入"实验中"

### Sprint 5.B 漫画态战略性降级(2026-05-18 拍板)
**决策保留**:B 选项 = 降级保留(不砍 / 不继续投入),保留代码 + 入口,UI 加 "⚗ 实验中" chip;等行业 AI 生图模型质变(预计 FLUX 2.0 / SD 4.0 / Wan 2.5,18-24 月内)再开放;主战场转回前三态深做。漫画态退出 credit 体系,改"订阅福利免费次数":Free 0 / Pro 1 / Max 2 / 超级 Max 4 本/月。

### Sprint C(商业模型)
- **C.1 Credit 配额重构(全栈)** — quota_service 瘦身 + credit_service 593 行 + billing_service 字段改 credit 模式 + 4 router 删 enforce_quota 调 InsufficientCredits → 429
- **C.5 credit cron + 提醒邮件 + admin 触发** — credit_cron.py / credit_reminders.py / scripts/run_credit_cron.py

### Sprint D(末尾态 + 漫创态 + API 路由)
- **D.3 AI 协作认证徽章** — BadgeDownloadDialog(浅/深 SVG 切换)
- **D.7.A 质量债 + CSS 虚拟化 + 3D 性能修复**
- **D.8 国产 LLM/绘图/多模态 API 路由层** — Adapter Pattern + Protocol;主路由 DeepSeek V3 / Doubao Seedream 3.0 / Qwen-VL Max;横评 5+3 用例全过(¥2.41 实测)
- **D.9 Sprint 2.A-4.D 漫创态** — 10 agent 流水线 + 漫画阅读器 + Typesetter PIL 整页排版 + 导出 PDF/ZIP + external 源

### Sprint 1.A-1.R(B/B+ 阶段)
- 1.A 脚手架 + SQLite + migrations / 1.B Auth(纯邮箱 OTP)+ JWT + Consent / 1.C Project/Character/Relationship/Event CRUD / 1.D Refine 服务 + §16 三兜底 / 1.E 前后端联调 / 1.G 续写引擎服务化 / 1.J 我的剧情线 / 1.N 永久保留承诺 / 1.O 滚雪球续写 / 1.Q composer.md v1 LOCKED 7 类语体 / 1.R 自洽守护者 + 8 维度

### Sprint 2.A-2.E(C 阶段中间态)
- 2.A 文件上传 + 红旗指纹 / 2.B 自动图谱抽取 / 2.C 反事实变量 + 三维度 / 2.D 正典守护者 / 2.E 去 IP 化导出

### Sprint 3.A 末尾态(D 阶段)
- 末尾态服务化 + composer.md 末尾态分支 + original_tail_excerpt 自动抽末尾 2000 字 + simulateButtonLabel 4 态文案 mapping

---

## 🛡 用户拍板铁律(2026-05-18)— 写进 CLAUDE.md 同步

> **以最高标准开发项目,不要遇难则退,不管多复杂都要做。**

落地含义(任何技术决策时回看):
- ❌ "工程妥协 / 工作量减半 / 取巧用现有架构应付" → 若产品差异化 ≠ 满分,拒绝
- ✅ "重写架构 / 引入复杂依赖 / 5-10× 成本" → 若是 doc 3 北极星(意难平救赎)所需,做
- ⚖ 遇到"A 快但伪 vs B 真但贵"二选一时,**默认走 B**;A 仅在 B 工程上根本不可行时备选
- 📜 不"借口 YAGNI 推迟" — YAGNI 仍生效,但仅针对**用户用不到的功能**;**核心差异化逻辑 = 必须做满**

**首次落实场景**:Sprint 6.A2 M3 多 agent 灵魂续写(2026-05-18)— 用户挑战"A 工程妥协"后,决策走 B(N agent 独立 LLM 进程 + 私有记忆 + RAG + 多场景轮转),工作量 0.5 → 2 sprint,LLM 成本 1× → 5-10×,**接受**。

---

## 项目身份

**浑晶 HunJing**(原代号"绘梦"已弃用)— 3D 知识图谱 + 多 agent 仿真平台,为虚构作品做剧情续写 / 重塑 / 创造。

**底层定位**(doc 3 锚定):给所有"被一部作品的结局伤过"的读者,一个救赎的工具。

**北极星指标**:意难平。任何功能不能解开读者的意难平,就砍掉。

**寓意**:浑然之初的故事变成可剖析的晶体。

---

## 创始人处境

- **独立开发**:无合伙人,一人全程操办整个创业过程
- **大学生身份**:法律 / 财务高密度信息会带来心理挫败;**原则:不追求律师级完备,追求"小作坊够用"**
- **资源稀缺**:经济能力仅够吃饱穿暖,无社会人脉
- **心理基准**:不被"现实考虑"稀释最初的情感连接;**意难平既是产品灵感,也是质量基准**
- **AI 沟通约束**:法律 / 财务话题压缩到"足以让项目活下去"的最小集合,不做高密度信息轰炸

## 产品野心

**不做 MVP,做完全体** — 不再用"先 ship 核心功能 X 留下个 sprint"作借口跳过功能。所有 doc 设计的功能(四态全部 + 正典守护者 + 角色对焦 + 自洽守护者 + 滚雪球 + 永久保留 + 去 IP 化导出 + 配额可视化 + 协议中心 + 3D 编辑器 + 暗色主题 等)都要做完再上线。

**部署是最后一步** — 不在功能未完整时部署。

---

## 四态产品形态(北极星)

底层引擎统一(知识图谱 + Director-Agent-Composer),四态只是入口和约束不同。

| 态 | 入口 | 核心动作 | 版权风险 | 当前状态 |
|---|---|---|---|---|
| **初始态** | 空白世界 | 用户**手动**从零创建角色 + 关系 + 事件 → AI 推演 | **零** | ✅ 完成(B 阶段) |
| **中间态** | 导入已有作品 | AI **自动**抽人物 / 关系 / 事件 → 用户在 3D 图谱里改角色属性 / 改事件参数 / 注入反事实 → 后续剧情重塑 | 高 | ✅ 完成(C 阶段:2.A→2.E 全套) |
| **末尾态** | 导入已有作品 | AI 自动抽图谱 → 不改原剧情,从末尾一键续写;后端从最近 ready upload 抽末尾 2000 字注入 director + composer | 高 | ✅ 完成(D 阶段 3.A) |
| **漫创态** ⭐(原"周期态"重定义,UI 短名;架构层全称"漫画创作态") | 已有文本(浑晶产物 / 外部) | **10 agent 流水线**(编剧 / 画风定调员 v2 多模态 / 角色锚定员 / 素材库抽取员 / 导演 / 生图 / 重绘 / 质检 / 排版)→ 自动批量生成漫画 | 高(AIGC 视觉 + 用户上传参考图版权额外备案)| 🧪 **工程内测**(D.9 Sprint 2.A 后端 4 agent + 2.B 前端 UI 完工;法务备案完成后对读者层正式开放;详 docs/ADR_漫画创作态架构.md **v3**)|

**Sprint 2.B+ 命名收尾(2026-05-12)**:UI 用户可见层把"漫画创作态"压缩为短名**"漫创态"**(2 字 vs 4 字,与"初始态/中间态/末尾态"3 字节奏更对齐;架构层文档 / 后端注释 / ADR 保留全称),涉及 `CreationModeQuadrant.title` / `NewProjectModal.label` / `MyComicsView` 提示文案。原则:**架构层全称(给开发者读),UI 层短名(给用户读)**。

漫创态**进入工程内测**(2026-05-12),前置 2/4 已达成(国产 API 路由 ✅ / 读者层验证 ❌);法务备案 + 公司注册完成后正式开放。后端 `ProjectMode` enum `'cycle'` 字面量保留,前端 UI 短名"漫创态"(语义层 vs 字面量层解耦)。

**漫画态隔离铁律**(产品架构级):
- 前 3 态 → 漫画态:**单向**(漫画态可选前 3 态产物作输入源)
- 漫画态 ╳ 跳回前 3 态(无入口)
- 前 3 态项目页 ╳ 暴露漫画态按钮(防生态污染)
- 漫画态产物**不进**「我的剧情线」,走独立「我的漫画」列表

**关键心智边界**(违反就是反人类设计):
- **手动加角色卡片是初始态专属**,套到中间 / 末尾态会让用户无所适从
- **中间 / 末尾态都靠 AI 抽图谱**,用户在 3D 图谱里"动刀"而非"建造"
- **漫画态消费前 3 态产物**(或外部文本),不直接抽图谱;走独立 8 agent 流水线 → 输出漫画,不走 simulation
- 任何前端改动触及 4 态边界前,必读 `项目文件/2:我对产品的初步描述.docx`

**重塑度阈值** 10-90% 对所有态生效,绑定三个物理维度:
1. 可改属性的角色数量上限(`reshape_to_max_touched_characters` = `max(1, ceil(percent/5))`)
2. agent 互动最大轮次(10% = 5 轮,90% = 50 轮)
3. **与改动节点的图谱距离**(公式 `floor((percent-10)/10)` → 0-8 跳 BFS,边集 = relationships + events.participants)

**主战场**:小说优先 → 漫画次之 → 视频暂不进入(2026 年成本做不到)。

---

## 产品差异化机制(完工矩阵)

| 机制 | 状态 | 关键 |
|---|---|---|
| **角色对焦**(reader's_canon_confirmation) | ✅ B 阶段 | AI 给 70 分雷区草稿 → 用户对每条赞同/反对/补充/调整严格度 → "这是你心中的 [角色名]";prompt LOCKED `prompts/character_focus.md` v3 |
| **自洽守护者**(self-consistency guardian) | ✅ B+ 阶段 1.R | 推演 done 后用户主动点诊断;8 维度看产物 vs 用户设定;不扣配额;prompt LOCKED `prompts/self_consistency_guardian.md` v1 |
| **正典守护者**(canonical_guardian) | ✅ C 阶段 2.D — **doc 3 评"竞品短期内难抄走"** | 与角色 agent 平行只审计不参与;审产物 vs 原作 canon;8 维度 + 4 档 severity;**反事实豁免铁律**(cf cover 的字段在审计中标 counterfactual_exempt=true 而非违规);prompt LOCKED `prompts/canonical_guardian.md` v1 |
| **滚雪球续写 / 长篇累积** | ✅ B 阶段 1.O | simulations 加 context_simulation_ids + narrative_summary;前文超 8000 字摘要化;DeepSeek V3 64K context |
| **AI 续写产物永久保留承诺** | ✅ B 阶段 1.N | 协议加段 + UI ♾️ 角标 + 配额限"次数"非"已存数量" |
| **3D 关系图谱 — 操作世界(不只是看)** | ✅ 形态 A/B/C 完成,形态 D 留 C/D 阶段 | A 编辑器 / B 对焦联动 / C 续写联动 / D 反事实推演沙盘 |
| **4 态心智锚定入口**(2x2 浅色专业卡) | ✅ B+ 阶段 2.A.F | DashboardView 三态布局 + ChooseModeModal + NewProjectModal mode prop;ProjectView 按 mode 分屏 |
| **反事实工作台 + 三维度** | ✅ C 阶段 2.C / 2.C+ | counterfactual_changes 表 + 显式创建 + world type 6 维度(genre/setting/magic_system/time_axis/tone/free_form)+ user_intent ★ 最高优先级 + selected_counterfactual_ids 子集 + BFS affected scope |
| **去 IP 化导出**(用户可选原版 / 去 IP 版) | ✅ C 阶段 2.E — **doc 5 四层版权防护核心层** | de_ip_dictionaries 表(per project 1 个)+ longest-match-first 替换算法(防"贾母"被"贾"前缀吃)+ 不扣配额;prompt LOCKED `prompts/de_ip_dictionary.md` v1;法务 trail 当前用 logger.info,未来加 db 表 `de_ip_export_logs`(不复用 violation_logs — 语义不对) |
| **AI 协作认证徽章**("由浑晶协助创作") | ✅ D 阶段 D.3(2026-05-12)| 2 SVG 变体(浅/深);BadgeDownloadDialog 浅深切换 + 预览 + 一键下载;SimulationDetailView done 屏「✨ 创作徽章」按钮;静态 SVG 托管 `frontend/public/badges/huimeng-badge-{light,dark}.svg`;markdown 导出附"> 由浑晶协助创作"。E.1 部署后再加公网 URL + markdown 嵌入代码 |
| **跨题材自适应语体**(composer.md v1 / Sprint 1.Q)| ✅ B+ 阶段 1.Q(2026-05-13 前)| 通用编辑级 prompt,根据作品名/类型/题材标签/角色名自适应 7 类语体(古典含蓄 / 武侠豪迈 / 科幻冷峻 / 现代都市言情 / 悬疑冷峻 / 温情治愈 / 黑色幽默);custom_style_hint 非空时最高优先级覆盖 |
| **末尾态笔法严格继承**(composer 末尾态分支 / m3_narrator 铁律)| ✅ D 阶段 3.A + 6.A2 M3.B | composer.md 末尾态分支:模仿原作语体/句长/用词/段落节奏;m3_narrator.md 铁律:有 original_tail_excerpt 时严格分析其用词/句长/节奏/视角并模仿,不复述原作末段、不引入反事实 |
| **灵魂续写多 agent 引擎**(M3 + M4 + M5)| ✅ 6.A2 M3-M5(2026-05-19/20)| agent_evolution_engine + 12 个底层服务(私有记忆/RAG/world_facts/canonical_entities/action_ledger/emotional_states/hard_constraints/multi-sample/consistency_checker/phrase_blacklist/round_visible/temporal_lock);治"实体覆写/动作循环/时空撕裂/角色情绪断裂"4 类瑕疵 |
| **Outline-First 长篇协作生成**(M6)| ✅ 6.A2 M6(2026-05-20)| 用户成为 outline 协作者:LLM 生成→用户审核/编辑/重生/继续生成→批准→按 outline 严格跑;治"剧情死循环 / 实体属性覆写 / 空间撕裂 / 段落级漂移";6 个 fix 修复实测瑕疵(截断/异步 UX/续生成/detail banner/进度感/严跑完所有幕)|

---

## 战略定位 + 商业模型(Sprint C.1 credit 重构,2026-05-13)

双层架构:
- **读者层**:免费 + Pro ¥138 — 叙事"意难平" — 获客豆瓣/晋江/贴吧/B 站同人圈
- **创作者层**:Max ¥438 / 超级 Max ¥1388 + 加购包 — 叙事"专业续写工作台" — 获客小红书/知乎/写作训练营

### Credit 单位 + 4 档(详 `docs/ADR_credit_quota_重构.md`)

**核心**:1 credit = ¥0.10 平台成本基线 × 1.3 毛利倍率 → Pro 单 c 售价 ¥0.1423
**阶梯优惠**:每升一档单 credit 售价 -10%(Pro 基准 / Max -9.5% / 超级 Max -10.2%)
**年付折扣**:严格月付 × 12 × 0.9(尾数 8 取整)

| 维度 | **Free** | **Pro** | **Max** | **超级 Max** | 创始人 |
|---|---:|---:|---:|---:|---|
| 月价 | ¥0 | **¥138** | **¥438** | **¥1388** | — |
| 年价 | — | **¥1488** | **¥4728** | **¥14988** | — |
| **月度 credit** | **30** | **970** | **3400** | **12000** | ∞ |
| 单 credit 售价 | — | ¥0.1423 | ¥0.1288 | ¥0.1157 | — |
| 阶梯优惠 | — | 基准 | -9.5% | -10.2% / 累计 -18.7% | — |
| characters_per_project | 10 | 30 | 50 | 100 | ∞ |
| projects_total | 2 | 5 | 20 | ∞ | ∞ |
| reshape_max_percent | 30% | 80% | 90% | 90% | 90% |

### 各动作 credit 消耗表(C.1 锁定)

| LLM 调用 | 真实单价 | credit |
|---|---:|---:|
| DeepSeek V3 1K input | ¥0.0010 | 0.013 c |
| DeepSeek V3 1K output | ¥0.0020 | 0.026 c |
| Qwen-VL Max 1K vision | ¥0.0200 | 0.26 c |
| Seedream 4.0 1 张 | ¥0.20 | 2.6 c |

| 业务动作 | credit |
|---|---:|
| 1 次 AI 推演(5M in + 0.5M out) | ~78 c |
| 1 次抽图谱 | ~14 c |
| 1 次角色对焦 | ~0.5 c |
| 1 张画风候选 | ~3 c |
| 1 张漫画格图 | ~2.6 c |
| 1 本 12 页漫画(72 格) | ~222 c |
| 1 本 30 页漫画(180 格) | ~510 c |

### 毛利结构(满配额视角)

| 档 | 满配额毛利 | 实跑 50% 毛利 |
|---|---:|---:|
| Free | -¥3(获客) | -¥1.5 |
| Pro | 29.7% | 65% |
| Max | 22.4% | 61% |
| 超级 Max | 13.6% | 57% |
| **加权(60/30/10)** | **26%**(20-30% 区间 ✓) | ~62% |

### 加购包(独立钱包,1 年有效期,所有档同价)

| 包 | credit | 价格 | 单价 | 毛利 |
|---|---:|---:|---|---:|
| 小包 | 100 c | ¥18 | ¥0.18/c | +80% |
| 中包 | 500 c | ¥85 | ¥0.17/c | +70% |
| 大包 | 2000 c | ¥320 | ¥0.16/c | +60% |

→ 加购最便宜大包仍比超级 Max 订阅贵 38%(¥0.16 vs ¥0.1157),**继续逼用户升档**

### 关键铁律

1. **不再涨订阅价 / 不再涨订阅 credit 池**(任何调动破坏阶梯优惠平衡)
2. **加购单价永远贵于订阅**(鼓励订阅,加购作"超额突发"用)
3. **订阅 credit 月末清零**(防囤积压成本);**加购 1 年有效**(信任锚 + 防财务负债)
4. **消耗顺序铁律**:优先扣 subscription wallet,不够再扣 addon FIFO
5. **创始人专档**:env `HUIMENG_FOUNDER_EMAILS` 白名单(默认 `javaspringcjiajia@foxmail.com`)→ `deps.get_current_user` 内存改写 plan='founder';PLAN_LIMITS["founder"] credit=9999999
6. **卖工具能力(credit),不卖创作权**(doc 5:Photoshop 不为用户改迪士尼海报负责)

**对外定调**:
- vs 通用 AI:我们懂原作世界观,它们写"AI 风格文字"
- vs MiroFish(doc 3 范式级差异):我们做"已发生的虚构",它们做"未发生的现实";我们的 agent 有名,它们的 agent 无名
- vs AI Dungeon:我们为已存在的世界续命,它们造新世界

---

## 架构选型

- **后端**:Python 3.11 + FastAPI + SQLite(阶段 3 切 PostgreSQL);**复用项目根 `.venv` + pip**(2026-05-09 决议,撤回原"uv"方案)
- **多 agent**:**自实现 Director-Agent-Composer**(`simulation_service.py` 状态机 + 异步 worker + SSE 流);原计划 fork OASIS 已废弃 — 实际开发时发现 OASIS 抽象层太重,自写 1681 行 service 反而更可控
- **图谱**:**直接 LLM 单次抽取**(中间态时跨章节聚合切 LightRAG)
- **记忆**:SQLite + FAISS;Zep Cloud 留扩展
- **前端**:Vue 3 + TS + Vite + Pinia + vue-router + 3d-force-graph + Three.js;Claude 风浅色 token(`--color-*` 主色浑晶紫 #7C3AED)
- **LLM 路由层(D.8 完工,2026-05-12)**:Adapter Pattern + Protocol 接口(`backend/app/services/llm_routing/`,详 `docs/ADR_D.8_国产API路由层.md`)
  - **文本**:DeepSeek V3 主路由(OpenAI 兼容,deepseek-chat / reasoner / V3 三档)
  - **图像生成**:Doubao Seedream 3.0(火山方舟 ARK API_KEY,`doubao-seedream-3-0-t2i-250415`)— 漫画态主战场;价格 ¥0.20/张 2K 画质;5.0 写实味重不适用
  - **多模态视觉**:Qwen-VL Max(阿里灵积 DashScope OpenAI 兼容,`qwen-vl-max-latest`)— 漫画态质检 agent + 角色一致性 L3 候选
  - **数据出境红线**:律师确认锁死;海外 LLM(Gemini / Claude / GPT-4V)仅作离线横评,严禁进生产管线
  - **主角 / 配角分层路由保留**(主角 V3,配角可降到 V3-Lite / Qwen-Turbo;通过 router 工厂切换 adapter,业务代码 0 改动)
  - **pricing.py 配置化**:vendor + model + unit_type → price 三元组查表,新加 vendor 改表不动代码

---

## 法务三条核心结论(2026-05 律师 1 轮咨询)

1. **对外无效,对内有效** — 平台必然作为共同被告,无法靠协议把责任切干净;但现有证据链(IP + 设备指纹 + 行为日志 + 订阅快照)够追偿,**无需上人脸识别**
2. **AIGC 算法备案 + 服务备案 3-6 月死线** — 比任何产品功能都更前置,**公司一注册就立刻启动**;迟于此线 = 平台违规上线
3. **数据出境红线** — 锁死 DeepSeek 主路由,海外 LLM 仅离线横评不进生产;同时锁死了"切 Claude/GPT 提质"的退路

**P0 公司层**(并行启动,均挂在 AIGC 死线下):上海公司注册 / AIGC 算法 + 服务备案 / ICP 备案 / 长期法律顾问签约。

---

## D 阶段开发避坑(2026-05-11 用户拍板)

末尾态 / 漫画态(D.9)进场时,这两类 bug 必须**在 sprint 设计阶段**就盯死(已在 C 阶段踩过):

1. **路由参数变化 → view 实例复用** — Vue Router 切 `/projects/A` → `/projects/B` 时不重建 ProjectView,只换 props.id。任何 `onMounted(loadAll)` 模式都是错的,必须用 `watch(() => props.id, loadAll, { immediate: true })`,或把 composable 签名改成 `() => string` getter(参考 `useProjectGraph` 2.E polish)。新视图 / 组合式函数接 projectId / simulationId 时,默认走 getter 模式。

2. **入口按钮 / 提示文案 mode-aware** — 用户 4 态心智:initial=AI 推演 / middle=AI 重塑 / end=AI 续写 / cycle=AI 长篇。任何写死 "AI 续写" 字面量的按钮 / toast / dock title / aria-label 都是 bug(用户在中间态点开「AI 续写」按钮,弹出来 modal 标题写「AI 重塑」=心智撕裂)。已落 `simulateButtonLabel` computed 模式(ProjectView + ProjectGraphView + SimulationDock 都走相同 mapping),新视图复用此约定。

3. **credit 余额提示用伞形术语「AI 创作」,不写死 mode 专属动词**(Sprint C.1 重定义) — credit 是跨 4 态 + 跨动作类型(refine / continuation / extract / comic_*)的统一钱包,任何余额 / 不足提示用「AI 创作」「credit 余额」中性词,**不要写「续写次数」「漫画态本数」**等次数模式残留。已落地于 QuotaIndicator + SimulationDock + legal-docs + UpgradeModal。

4. **mode-specific UI 元素显示规则**(末尾态 3.A 拍板) — 不同 mode 应该看到不同的 UI 元素,不能"是非初始态就显":
   - **反事实工作台按钮 / 反事实图谱标记** — 仅 `middle` 显示(末尾态语义是"不动原作",反事实工作台与心智冲突;漫画态走独立流水线,无 simulation,故也不显)
   - **作品文件上传区** — `middle` / `end` 显示(初始态不导入作品;漫画态走独立创建流程)
   - **3D 图谱顶栏「反事实」chip** — 仅 `middle`
   - **SimulationDetailView「原作末段」预览** — 仅当 `simulation.original_tail_excerpt` 非空(末尾态创建的 sim 才有)
   - **顶栏「AI ___」按钮文案 + dock 标题** — 4 态各自 mapping(`simulateButtonLabel` computed)
   反例:`v-if="!isInitialMode"` 是错的二元判断,会把末尾态错放到中间态的 UI 路径(末尾态显反事实工作台 = bug)。新加 UI 元素时,显式列出"显示在哪些 mode"白名单。

5. **router-handled error code → frontend toast 映射** — 任何抛 422 的新业务错误(本 sprint 加了 `END_MODE_NO_UPLOAD`),必须在 SimulationDock(或对应 dock 组件)的 `watch(errorCode)` 里加 case + 友好 toast 文案。**不要让用户看见 raw error code**。已落地约定:`QUOTA_EXCEEDED` / `RESHAPE_CHARACTER_LIMIT_EXCEEDED` / `END_MODE_NO_UPLOAD`。

6. **prompt 注入区不要混淆"原作正文" vs "浑晶产物"** — 末尾态(3.A)加了 `original_tail` 独立 section(铁律:不复述、模仿语体、不引入反事实);**不要复用**滚雪球(1.O)的 `prior_context`(那个是浑晶生成的前序产物,语体可能已变)。两个 section 措辞要严格区分,避免 LLM 把"原作末段"当 derivative 处理 → 续写时擅自改写原作。

7. **每完工一个新 mode sprint,必须同步更新所有"mode 白名单"** — 项目里有 4 个地方写死了"哪些 mode 已上线"的硬编码,完工新 mode 时必须**全部**改,否则用户能在 quadrant 点到该 mode,但下游流程因白名单 if 没匹配上而静默丢弃 → 体验是"点了没反应,被踢回主页"。完工 checklist:
   - **① `CreationModeQuadrant.vue` 的 `MODE_CARDS`** — `status` 改 `"available"` + `statusLabel` 改 `"可用"`;`handleClick` toast 文案补充新可用 mode 名
   - **② `DashboardView.vue` 的 `onModeSelected`** — `if (mode === "initial" || "middle" || "end" ...)` 白名单加新 mode,否则 `newProjectModal.open` 不触发,用户回到主页
   - **③ `NewProjectModal.vue` 的 `MODE_META[<新 mode>].flowHint`** — 从 `"(开发中)"` 换为实际流程描述
   - **④ `ProjectView.vue` 的 `readyCardDescription` computed** — 加新 mode case(影响"图谱已就绪"卡片描述)
   - **⑤ 验证 `SimulationDock.dockHeaderConfig` 已覆盖该 mode**(initial / middle / end / cycle 4 case 都在)
   - **顺手**:`projectMode === 'middle'` 这类反事实工作台显示条件,看是否要加新 mode(取决于产品语义)

8. **新建项目后的引导 toast 已废弃 — 不要再加 mode-specific 浮窗提示** — 操作禁用态用 `aiActionsDisabledReason` 的 `title` tooltip 兜底("请先在「作品文件」区上传文件...");toast 占据顶部 stack 空间,挡其他系统提示。新加 mode 时**禁止**在 `App.vue` 的 `handleProjectCreated` 里再写 `toast.info(...)`;视觉引导走 ProjectView 内部 UI 元素。

9. **migration 加完必跑 `python backend/scripts/init_db.py`** — 纪律 5 早就警告过,但 3.A 仍踩过(用户业务 DB 没升 → "table simulations has no column named original_tail_excerpt" 报错)。pytest conftest 每次自动重建 test DB,误导工程师以为"我跑了 pytest 全绿就行" — 但**用户业务 DB(backend/data/huimeng.db)不会自动升**。新加 migration 后,本地开发 DB / 演示环境 DB 都要手动跑 init_db。**Sprint 6.A2 FOCUS.10(2026-05-22)起 app.main 启动钩子自动跑 migrations**,但仍建议手动跑确认。

10. **末尾态创建推演不应让用户写"锚点描述"**(3.A polish 1) — 末尾态语义是"AI 继承原作作者意志自然延续",用户写锚点 = 用户意图污染。前端 SimulationDock 在 `isEndMode` 时隐藏 divergence 输入,提交时自动填中性文案(`END_MODE_DEFAULT_DIVERGENCE`)。判定锚点要不要显示的规则:**"用户在这个 mode 下应该有意图表达吗?"**(initial / middle 是 — 用户主导;end 否 — AI 模仿原作主导)。漫画态(后端字面量仍 `'cycle'`)不走 simulation,无锚点概念。

11. **关系强度阈值机制是产品级数据契约**(3.A polish 2,doc 3 拍板) — LLM 抽图谱时给每条关系打 5 级 strength enum(strong / mod_strong / moderate / mod_weak / weak);前端 `RELATIONSHIP_STRENGTH_SCORE` 映射到 10/30/50/70/90;`projects.graph_strength_threshold` (10-80) 决定显示门槛。默认 30 过滤 weak(对应"五等级里弱不显示"语义,保留 top 80%)。**关键产品语义**:孤立角色不再是"AI 抽错了"或"精度不够",而是"这个角色与其它角色弱相关 — 阈值 ↓ 滑块会让它们显出来"。该机制要求:① LLM 必填 strength;② DB 字段不能丢;③ 前端 transformBackendGraph **必须**走 score < threshold 过滤,不能直接跳过该层逻辑。

12. **前端事件流的 main/sub 双列布局**(3.A polish 3) — ProjectUploadsPanel 的 `event-row` flex 布局踩过:`event-main { flex: 1 }` + `event-sub { flex-shrink: 0 }` 在 sub 文本超长时,sub 占满几乎全行,main 被压到极窄 → `word-break: break-word` 让中文每字独立成行。已修:main 加 `min-width: 0`,sub 改 `flex: 0 1 auto` + `min-width: 0` + `max-width: 50%` + ellipsis。**未来加新事件 kind 时**:若 sub 内容可能很长(如名单 / 长描述),要么先在 useExtractJob 格式化时截断,要么靠这层 CSS 兜底。

13. **"用户期望入口"与"实际入口"的体验断层防御** — 一个机制的入口位置由它的**真实语义**决定,不是"用户在哪里会想起它"。如果两者错位,要在**用户会想起的地方加说明卡片**,而不是把入口搬过去(否则用户对该机制的心智会被误导)。
    - **反例**:阈值机制的入口在 3D 图谱顶栏(语义:显示密度);但用户在抽图谱面板会问"为什么这里没阈值"(心智:AI 抽取精度)。**正确做法**:抽图谱面板加 `threshold-explain` 说明卡片,把产品语义讲清(AI 不漏抽 + 入口在 3D 图谱),不加滑块。
    - **判定模板**:看到用户问"这里为什么没有 X 控件"时,先问自己:**X 的真实语义,是不是属于这个场景的?** 如果是 → 加控件;如果不是 → 加说明卡片告诉用户 X 在哪 + 为什么不在这。

14. **LLM 抽取过保守(密度坍缩)的两层防御** — `build_graph` LLM 默认倾向于"只抽明显的强关系"。两层兜底:
    - **第一层(已落)**:prompt 给数量锚 + 抽取自检 checklist + "不确定按高一档"。具体见 `prompts/build_graph.md` v6
    - **第二层(未来 polish 备用)**:后端 extract_service 合并完 chunks 后,扫描所有 PERSON,**零关系角色自动补一条到"最 hub PERSON"的 "提及" + weak**(防 prompt 仍漏)。当前未实现,因为可能引入语义错误的伪关系;只有 prompt 改完仍发现孤岛 ≥ 5% 时再加。
    - **新加 prompt 要素时**:任何"判定 / 取舍 / 数量"的指令,要给具体数字锚(如 "≥ N × 1.5")而不是模糊修辞("尽量多")— LLM 对数字锚响应度比修辞高一个量级。

15. **prompt 升级后用户需主动重抽** — `build_graph.md` v2 → v3 这类升级**只影响新抽取**,老项目的图谱仍是 v2 版本输出。用户能通过 ProjectUploadsPanel 的"重抽"按钮(ready 状态文件可点)应用新 prompt。**告诉用户这一点是 sprint 完工的必动作**(否则用户调阈值滑块仍看不出区别,因为图谱是老版本的)。

16. **Vue template `:attr="..."` 三元 / 复合表达式禁内嵌双引号字面量** — D.2.A 踩过(2026-05-12):写 `:title="cond ? 'A' : 'B 含 \"操作世界\" 字样'"` 看似没错但 vite:vue parser 撞到内嵌 `"` 报 `Unterminated string constant`。修法两条任一:
    - **(推荐)** 拆 computed:`:title="myTitle"` + script 区 `const myTitle = computed(() => cond ? "A" : "B 含 \"...\" 字样")`
    - 内嵌单引号或全角引号「」/ Unicode 双引号""
    - **判定**:template 内表达式只要长 / 含中文 / 含特殊符号 → 拆 computed,别贪图省事。**vue-tsc 这层不报这个 parser 错** — 必须实际启动 vite dev server 才能撞见。

17. **新视图 requiresAuth 默认策略 = false + view 内部自管**(D.9 Sprint 2.B+ 踩过,2026-05-12):
    - 用户报告"点漫创态卡跳主页",根因是 /my-comics 配 `requiresAuth: true`,token 失效或 isAuthed 竞态时 `beforeEach` **静默 redirect** 到 /dashboard,体验像"点了没反应"
    - **新规则**:**列表 / 列表入口型 view** 默认 `requiresAuth: false`,在 view 内部用 `auth.isAuthed` gate 渲染"游客 hero CTA",CTA 点击 → `loginModal.open(to.fullPath)`。优势:
      - 任何入口(sidebar 直达 / 4 态卡片 / 收藏夹 URL)都不会被静默吃掉
      - view 视觉上一致(游客也能看到"这是什么页面"的语义解释)
      - 401 catch:`if (e instanceof ApiError && e.status === 401) { comics.value = []; }` 走未登录 hero,不显 raw error
    - **保留 `requiresAuth: true`** 的:**详情页**(/comics/:id / /projects/:id / /simulations/:id)
    - **双保险**:在入口逻辑(如 DashboardView.onModeSelected / AppSidebar.gotoMyComics)也加 `if (!auth.isAuthed) { loginModal.open(targetPath); return; }`
    - **判定模板**:看到新 view 的 router config,问自己:"游客打开这条 URL 会看到什么?如果是空白被踢主页,改 requiresAuth: false + view 加 hero"

18. **3d-force-graph `.linkOpacity(accessor)` 不是 reactive — 必须手动强制更新 link material**(D.2.B 踩过,2026-05-12):
    - `.linkOpacity(fn)` API 在 line 创建时调 fn 一次,把结果写到 `LineBasicMaterial.opacity` 然后**缓存,不再重算**
    - props 变化(如 sandboxMode 切换)→ accessor function reference 没变 → force-graph 不知道要刷 line material
    - `graphData(d)` reload 重建 line → 创建路径可能也不调 accessor(force-graph 内部优化用 cached default)
    - **解决**:`forceUpdateLinkOpacity()` 手动迭代 `graph.graphData().links` 拿 `link.__lineObj.material`,改 `.opacity` + `.transparent` + `.needsUpdate=true`
    - **触发时机**(D.2.B 双层保险):
      - `watch(() => props.sandboxMode)` → 立即调
      - `graph.graphData(d)` reload 后 `setTimeout(forceUpdateLinkOpacity, 0)`(等 force-graph internal sync)
      - `.onEngineStop()` 兜底
    - **触类旁通**:任何接 force-graph reactive prop accessor(nodeColor / linkColor / nodeOpacity / linkWidth 等)都有同样问题,需要手动 forceUpdate。

19. **mode-aware 文案 = 用户视角描述 / 中性文案 = 引擎视角描述** — 3.A 修过几轮 "推演中/失败/完成" 写死的问题:
    - **mode 专属(用户视角)**:按钮文案 / dock 标题 / 失败 dialog 标题 → 跟 `dockHeaderConfig.title` / `modeActionLabel` 走(AI 推演 / 重塑 / 续写 / 长篇)
    - **中性(引擎视角)**:state label map(queued/directing/composing/done/failed/cancelled) / 事件流单行 main 文案 / api 错误兜底文案 → 用 "排队中 / 创作中 / 编织中 / 已完成 / 已失败 / 已取消" 这种**与 mode 无关**的描述
    - **关键判定**:这条文案"如果项目是末尾态,用户看到时会不会觉得错位"?
      - 会 → 走 mode 专属
      - 不会 → 用中性词

20. **大规划方向延后判定 = 当下 ROI 与 4 红线** — 用户提出大胆方向(如漫画态)时,工程师不该立刻动手开发,也不该简单砍掉,而是按 4 维红线评估:
    - **数据出境红线**:依赖的 LLM / 绘图 / 多模态 API 是否境外?若是,有无境内替代?切换有多重?
    - **法务备案边界**:是否需在原 AIGC 备案外追加新备案(如视觉模块)?
    - **工程量 vs 当前 phase ROI**:工作量是否 > 当前 phase 剩余工作量的 2x?若是,延后到下一 phase
    - **触发条件清单**:列出"达成什么条件才该启动开发",闭环 — 任一未达成不动手
    - **正确产物**:写 ADR 文档(`docs/ADR_*.md`)锁定决策,前端用提示性 UI(如 "开发中 — 等 X 完成后启动")给用户预期,后端不做大改动

21. **"踢回 X view 让用户走 Y 路径"= UX 死锁陷阱**(D.9 Sprint 2.B+ 二修踩过 2026-05-12,致命 bug):
    - **反例**:MyComicsView 空态"新建漫画"按钮原逻辑 `toast.info("去 dashboard 选 4 态") + router.push("/dashboard")`;但 dashboard 的 4 态 cycle 分支自身又 `router.push("/my-comics")` → **A→B→A→B 死循环**
    - **判定铁律**:**view 的功能必须就地完成**。如果 sprint 简化想"踢去另一个 view 处理",必须立刻验证"另一个 view 处理完会不会让用户反踢回来" — 只要存在闭环可能就是死锁,**不准上线**
    - **正确做法**:用户在 view 内点 CTA → 在 view 内弹 modal / dialog / panel 就地处理。模态归零(背景虚化 + 中央卡),不跳路由,完成后只往**详情页**方向(单向)emit
    - **额外硬约束**:任何"Sprint X+ 加 modal 替代当前 toast 引导"的债,**不准跨 sprint 拖**

22. **整数百分比单位约定 — 避免 schema 改动触发架构闸门**(D.9 Sprint 2.B+ 四修落地 2026-05-12):
    - **场景**:用户要求漫画 cancel 按进度退款 — 10-80% 取消扣半本配额。但 `usage_logs.quota_consumed` 是 INTEGER NOT NULL,半粒度(0.5 本)无法直接表达 → 改 schema 加 REAL 列触发架构闸门 + ADR
    - **方案**:在**单一 action 路径下约定整数单位换算**(漫画 100=1本 / 50=半本),其他 action(refine / continuation / extract)语义独立保留 1=1次。**不动 schema**
    - **铁律**:任何"现有字段语义不支持新需求"的场景,先看能否**在单一路径下约定单位换算**,再考虑改 schema。换算约定要在**代码注释 + 项目记忆**双重沉淀,避免后续维护者误用 raw 整数语义
    - **反方向**:若一个新需求涉及**多个 action 都要半粒度**(如以后漫画 + 视频 + 音频都要按时长计费),则统一改 schema 加 REAL 列更划算 — 这时再触发架构闸门

23. **Teleport modal 卸载与 router.push 的 race condition**(D.9 Sprint 2.B+ 五修踩过 2026-05-12):
    - **症状**:modal 提交成功 → emit('created') 通知父 → 父 `createComicOpen=false` + `router.push(详情页)` → toast 出 + modal 关 + **URL 没变,navigation 被静默吞**
    - **根因**:emit 同步触发父 handler;父先做 reactive(close modal)再 push。reactive flush 触发 `<Teleport>` 卸载,与 navigation 进入新组件树同帧发生 → race condition,Vue Router 4 silent NavigationAborted
    - **铁律 — modal 内自管 navigation,不让父通过 emit 跳转**:
      - modal 知道自己关闭时该去哪 → 直接 `useRouter()` + `await router.push()` 在 success path
      - emit('created') 仅作扫尾通知,**不承担 navigation 主权**
      - 父 handler 只做 close + reload 等 idempotent 操作
    - **兜底**:push 用 try/catch 包,NavigationFailure(duplicate / aborted / cancelled)console.warn 不抛

24. **vue-tsc 编译通过 ≠ ESM 运行时通过 — import 必须验证源 module 真的 export**(D.9 Sprint 2.B+ 六修踩过 2026-05-12):
    - **症状**:`useComic.ts:14` 写 `import { api, ApiError } from "../api/client"`,vue-tsc 0 错,但浏览器运行时炸:`SyntaxError: The requested module '/src/api/client.ts' does not provide an export named 'ApiError'`
    - **根因**:`client.ts` 内部 `import { ApiError } from "./types"` 自用,**但没 re-export**。TypeScript 编译器宽松,浏览器 ESM 严格按 `export` 关键字解析,运行时报错
    - **铁律**:**类型 / 工具类(class / interface / enum / 常量)统一从定义源 module 导入,不从中转 module 导入**
      - ✅ `import { ApiError } from "../api/types"`(真正定义处)
      - ❌ `import { ApiError } from "../api/client"`(client 只是 import 自用,没 re-export)
    - **防御**:加 import 时**点开源 module 确认有 `export`**,别凭"我看到这个文件里有这个名字"判断

25. **JS / TS 字符串字面量内嵌中文文案的双引号必须用「」或全角""**(Sprint C.1 legal-docs.ts 踩过 2026-05-13):
    - **症状**:`body: "...所有 credit 消耗在调用前会有"预计消耗"提示..."` → Vite oxc parse error `Expected `,` or `}` but found `Identifier``,因 `"预计消耗"` 把外层 `"..."` string 提前截断
    - **铁律**:JS / TS 字符串内嵌引号必须**显式区分**:
      - 外层 `"..."` → 内层用 `\"` 转义 OR 中文「」/ 全角"" / 单引号 `'`
      - 中文文案首选**「」**(直角引号,中文标点专用,不冲突 + 视觉清晰)
    - **触类旁通**:任何中文文案进 JS / TS / JSON 字符串前,**自动扫一遍内嵌引号**,把所有 unescaped `"` 改成「」

26. **LLM 单次 output token 硬限不能用 `max_tokens` 调大,必须分批承接**(Sprint 3 Phase 2 落地,2026-05-13):
    - **症状**:`_agent_scripter` 写 30 页 / 14 页 / 12 页(char 22204)漫画 → JSON 在 char 21K+ 处 `Unterminated string` 截断 → `LlmJsonParseFailed`
    - **根因**:DeepSeek V3 单次 output 硬限 ~8192 tokens(≈ 12K 中文字符);72 格 JSON 必定 > 20K 字符 → 必截
    - **错误修法**:调大 `max_tokens=16000` → API reject;降页数治标不治本
    - **正确修法**:**真分批承接** — 加 `_scripter_batch(batch_start_page, batch_end_page, total_pages, is_first_batch, is_last_batch, previous_tail_context, ...)` 原子调用;按 ceil(target_pages / 6) 切批 loop,每批 ≤ 6 页 × 6 格 = 36 格 panels,JSON ~10-12K 字符;批间通过 `tail_context` 承接;orchestrator 拼装
    - **判定铁律**:任何"用户可调输出规模"的 LLM 调用,**先算单次 output token 上限**,超 8K 就必须分批
    - **触类旁通**:同样适用 `extract_graph` / `_agent_visual_assets_extractor`(已截 30K 字)

27. **vendor 切换三字段必须同步,任一错配 → 401 silent fail 伪装成"慢"**(Sprint 5.4.1+ 实测,2026-05-13):
    - **症状**:用户切 `HUIMENG_JIMENG_MODEL=Kwai-Kolors/Kolors`(SiliconFlow 免费模型)但 `HUIMENG_JIMENG_API_BASE` 没改;用户感受是"AI 生图慢"
    - **根因**:火山方舟收到非自家 model + 非自家 key → 401 → adapter retry → 用户以为慢
    - **判定铁律**:**vendor = api_key + api_base + model 三元组,必须三字段同源**:
      - 火山方舟:sk-ARK key + `ark.cn-beijing.volces.com` + `doubao-*`
      - SiliconFlow:sk-SF key + `api.siliconflow.cn/v1` + `Kwai-Kolors/Kolors` 等
      - DashScope:sk-Ali key + `dashscope.aliyuncs.com/compatible-mode/v1` + `qwen-*`
      - DeepSeek:sk-DS key + `api.deepseek.com/v1` + `deepseek-chat`
    - **触类旁通**:任何 OpenAI 兼容 vendor 切换,默认假设字段名相同但参数键名可能不同;切 vendor 时**不能用"慢"反推"快"** — 慢可能是失败 retry

28. **sync IO 必须 timeout + 并发 + 累计上限,否则 Ctrl+C 也救不了**(Sprint 5.x 实测,2026-05-14):
    - **症状**:用户导出 PDF "大半天没结果",按 Ctrl+C 关后端也关不掉
    - **根因链(三重)**:① typesetter 内 **串行** 下载 6 panel,12 页 = 18 分钟;② `_export_comic_pdf` **无累计 timeout 上限**;③ anyio worker 跑 sync,**asyncio cancel 无法穿透阻塞 IO** → Ctrl+C 失效
    - **判定铁律**:任何 sync agent 必须 4 件齐备
      - ① 每个外部 IO 有合理 timeout(5-10s)
      - ② **同级 N 个 IO 必须并发**(`ThreadPoolExecutor`),不能 for-loop 串行
      - ③ 长流程加 **累计 timeout 上限**(wall-clock,如 90s),超时返 partial 不死等
      - ④ 关键步骤加 logging.info(用户能在后端 console 看到进度)
    - **强制关后端 server 的方法**(给用户):Ctrl+C **两次**(uvicorn 第 2 个 SIGINT 强制退出)/ `taskkill /F /IM python.exe`(Windows 暴力关)

29. **LLM "严格 JSON" 提示永远不可靠 — parser 必须能从任意 markdown 混杂里挖 JSON**(Sprint 5.x 实测,2026-05-14):
    - **症状**:DeepSeek V3 在 text_only 模式下违反"严格 JSON 不要前后文 / markdown 包裹"铁律,加了 markdown 标题前缀和 ` ```json ... ``` ` 包裹
    - **根因**:LLM 倾向于在响应里加"推理过程"/"分阶段说明",**强模型也常违反,弱模型必然违反**
    - **修法**:`_openai_compat_call_json` 3 级 fallback 解析:① `_strip_markdown_fence` 剥首尾 fence;② `_extract_json_block` 括号匹配挖 JSON;③ `_repair_llm_json` 双花括号修复
    - **判定铁律**:**任何调 LLM 期望 JSON 输出的端点,parser 必须能从任意 markdown 混杂里挖 JSON**。不能信赖 prompt 自律

30. **sync router 内 agent 阻塞期间前端必须主动 polling — 不能依赖 await response**(Sprint 5.x 实测,2026-05-14):
    - **症状**:用户报"dialog 一直显示 ⏳ 请稍候,以为网卡;进度条 85% 不动,退出再进才推"
    - **根因**:`useComic.load()` polling 触发条件 `isInProgress(state) && is_alive` 双闸门;sync router 内调 agent 不进 `_RUNNING_COMICS` → `is_alive=False` → polling 永不启动
    - **判定铁律**:
      - ① `useComic.load()` polling 触发**只看 isInProgress(state)**,不要叠加 is_alive
      - ② Dialog 期间(await sync router 阻塞)前端必须**主动启动 polling**(独立 setInterval)
      - ③ 提供 `startPolling()` / `stopPolling()` 显式 API
    - **触类旁通**:任何 sync 长流程的等待 UI,前端必须显式 polling 状态变化

31. **prompt 层"硬铁律" ≠ 工程层强制 — LLM 永远会违反规则,后端必须程序兜底**(Sprint 5.7 → 2026-05-14 沉淀,**漫画态质量根本教训**):
    - **症状**:Sprint 5.7 写了 screenwriter v3 12 铁律,prompt 强调"违反整次输出作废",但用户实测产物**仍全是大头照**
    - **根因**:**prompt 是建议,不是强制**。LLM(包括 DeepSeek V3)对 prompt 的执行率 60-80%,**永远有 20-40% 违反**;弱模型如 Qwen-Image 对专业摄影术语理解率更低(可能只 30-50%)。**靠 prompt 自律是工程偷懒**
    - **判定铁律**:任何 prompt 写了"必须 / 严格 / 违反作废" 的规则,**后端必须有程序级验证 + 兜底**:
      - ① 输出后 schema validation(检查规则是否真满足)
      - ② 不满足要么 retry LLM(在 prompt 头部加"上次违反了 X 规则") 要么 **程序直接改**
      - ③ 生图后调 VLM 验证("画的是不是 wide shot?有几个角色?"),不对就重生
    - **触类旁通**:任何 LLM 输出的"约束字段"都按"prompt 规则 + 程序验证 + 兜底改写"3 层走;**不可单层依赖 prompt**

---

## 七条执行纪律

1. **不用 "deferred / 留下个 sprint" 跳过设计中的功能** — 唯一允许跳过理由是"明确 YAGNI 且无后续依赖"
2. **每个新功能必须 trace 完整交互场景**(状态/事件/焦点 矩阵),不只是 vue-tsc / pytest
3. **改动前先读 `.claude/skills/` 里相关 skill**(frontend-specialist 触发 .vue/.css;code-reviewer 触发审查任务)
4. **用户提的产品级新需求要进路线图独立 sprint,不可硬塞当前 sprint** — 判断标准:**改一行公式 ≠ 产品级**;**改数据模型 / 改用户心智 / 增加新差异化 = 产品级**
5. **加 migration 必须主动跑 `init_db.py`** — pytest conftest 自动重建测试 DB,但用户业务 DB 不会自动升;否则会出 "no such table" 错误。**FOCUS.10 起 app.main 启动钩子自动跑**,但仍建议手动验证
6. **触及 4 态边界的设计前必读 `项目文件/2:我对产品的初步描述.docx`** — 4 态心智边界不能违反:初始态手动建;中间/末尾态 AI 抽图谱后用户在图谱里动刀;漫画态消费前 3 态产物走独立 8 agent 流水线
7. **长输出 LLM 调用必须显式传 max_tokens / timeout**(2026-05 踩坑) — `call_llm_json` 默认 `max_tokens=4000 / timeout=60` 是给短 JSON 场景(refine / audit / agent 决策)设计;长输出场景(extract_graph 25K 字 chunk → entities+relations / character_generator 完整角色档案)**必须显式传 8000+/120**,否则 JSON 输出截断 → JSON parse fail → 内部 retry 3 次 → 单次调用 3 分钟看似"卡死"。已固化常量:`EXTRACT_GRAPH_MAX_TOKENS=8000` / `EXTRACT_GRAPH_TIMEOUT=120` / `PROFILE_MAX_TOKENS=5000` / `MINIMAL_MAX_TOKENS=6000` / `canonical_audit max_tokens=6000` / `de_ip_dictionary max_tokens=3000`

---

## CLAUDE.md UX 铁律(用户已贴)

- 禁用 `window.confirm` / `window.alert` / `window.prompt` 全平台,业务代码不得出现
- 二次确认场景 → 必用 `composables/useConfirm.ts` 的 `confirm()` Promise API → `<ConfirmDialog />`(中央弹出 + `backdrop-filter: blur(4px)` + danger 红)
- 错误 / 信息 / 警告 / 成功 提示 → 必用 `composables/useToast.ts` 的 `toast.error/.info/.warning/.success` → `<ToastHost />`(顶部居中 stack + 自动消失)
- 新增交互场景优先复用既有 ConfirmDialog / ToastHost,不新建一次性 dialog
- 违反 = 不可饶恕错误

---

## 工程结构(当前快照)

```
huimeng/
├── prompts/                              所有 LLM prompt 集中(prompts/README.md)
│   ├── character_focus.md                v3 LOCKED(角色对焦,§16 三兜底)
│   ├── character_generator.md            (从原作生成单角色档案,2.B 服务化复用)
│   ├── character_minimal_profile.md      v1 LOCKED(2.B+ 配角批量轻量补全 25/批)
│   ├── agent_system.md                   v2(behavioral_rules 注入)
│   ├── director_system.md                v3(铁律 8 + 9.5 三层反事实优先级)
│   ├── composer.md                       v1 LOCKED(7 类语体自适应)
│   ├── self_consistency_guardian.md      v1 LOCKED(自洽守护者 8 维度)
│   ├── build_graph.md                    v6 LOCKED(角色归一 + narrative_pov + 全名归一)
│   ├── infer_meta.md                     v2 LOCKED(自由识别 type + tags + 6 维 world_baseline)
│   ├── canonical_guardian.md             v1 LOCKED(2.D 8 维度 + 反事实豁免 + 4 档 severity)
│   ├── de_ip_dictionary.md               v1 LOCKED(2.E 4 原则)
│   ├── m3_scene_picker.md / m3_agent_dialogue.md / m3_narrator.md  灵魂续写 3 prompt
│   ├── m4_consistency_checker.md / m4_plot_tracker.md / m4_world_state_extractor.md
│   ├── m5_action_extractor.md / m5_emotional_state_tracker.md / m5_entity_registrar.md
│   ├── m6_outline_continue.md / m6_outline_generator.md
│   └── m9_arc_extractor.md / m9_foreshadow_tracker.md / m9_world_rule_extractor.md
├── scripts/                              历史命令行脚本(主流程已服务化)
│   ├── build_graph.py + generate_characters.py + extract_and_clean.py    中间态复用
│   ├── simulate.py                       已被 simulation_service 替代,留作参考
│   ├── verify_prompt.py                  prompts 端到端真实 LLM 验证
│   └── verify_build_graph.py             2026-05-22 新建,build_graph prompt verify 工具
├── backend/
│   ├── pyproject.toml                    依赖声明
│   ├── app/
│   │   ├── main.py / config.py / db.py / deps.py(含 founder runtime promote + auto migration)
│   │   ├── models/                       30+ dataclass(各表 from_row + 派生)
│   │   ├── schemas/                      Pydantic schemas(每路由 1 个)
│   │   ├── routers/                      19 个:auth / consent / projects / characters / relationships / events / refine / quota / simulations / outlines / audits / uploads / extract_jobs / scenes / counterfactuals / counterfactual_combinations / canonical_audits / de_ip / billing / credit / comics
│   │   └── services/                     55+ services:auth / otp_email / refine / llm_client / project / quota / simulation / audit / file_parser / red_flag / upload / llm_extract / extract / counterfactual / counterfactual_combination / canonical_guardian / de_ip / credit / billing / credit_cron / credit_reminders / comic / agent_runtime / agent_evolution_engine / agent_profile_enricher / character_affinity / character_merger / consistency_checker / scene_extractor / scene_planner / chain_context_builder / long_form_mind_extractor / plot_tracker / world_state_extractor / action_extractor / emotional_state_tracker / entity_registrar / hard_constraints / phrase_blacklist / temporal_lock / outline_editor / outline_generator / outline_orchestrator / protagonist_judger / red_flag_filter / sequel_character_sync / sequel_scene_sync / relationship_phase_service / tail_style_analyzer / tail_style_checker / rag_retrieval / audit_acceptor_service / llm_routing/(adapters/ + pricing.py + protocols.py + router.py)
│   ├── migrations/                       56 个 SQL DDL(001-056,手工版本号,启动时 main.py 自动跑)
│   ├── seed/red_flag_seed.json           41 条宽松文学优先红旗词
│   ├── data/huimeng.db                   SQLite(.gitignore)
│   ├── data/uploads/{user_id}/           上传文件存储(.gitignore)
│   ├── data/comic_refs / data/composed   漫画参考图 + 整页 PNG(.gitignore)
│   ├── scripts/init_db.py                跑 migrations + seed red_flag
│   ├── scripts/run_credit_cron.py        Linux crontab / Windows 任务计划入口
│   └── tests/                            45+ pytest,**505/505 PASS**(2026-05-22)
└── frontend/
    ├── package.json                      vue 3.5 / pinia / 3d-force-graph / three / naive-ui
    ├── vite.config.ts                    /api → :8000 proxy
    └── src/
        ├── App.vue                       sidebar + main + 全局 modal 挂载(LoginModal / NewProjectModal / DocumentViewer / UpgradeModal / AddonPurchaseModal / ConfirmDialog / ToastHost)
        ├── api/                          client.ts(fetch wrapper + FormData 支持)+ types.ts(后端 schema 镜像)
        ├── stores/                       auth / consent / quota(Pinia)
        ├── composables/                  18 个:useLoginModal / useNewProjectModal / useDocumentViewer / useUpgradeModal / useGraphTheme / useProjectGraph / useRefineSession / useSimulation / useAudit / useExtractJob / useCounterfactuals / useCanonicalGuardian / useConfirm / useToast / useTheme / useComic / useAddonModal / useCreditEstimate
        ├── constants/legal-docs.ts       LEGAL_DOCUMENT(协议合并版,含永久保留承诺段)
        ├── components/                   40+ 组件:AppSidebar / LoginModal / NewProjectModal / DocumentViewer / UpgradeModal / QuotaIndicator / AddonPurchaseModal / CreationModeQuadrant / ChooseModeModal / CharacterFocusModal / SimulationDock / NarrativeStream / CrystalGraph(可编辑 + AI 联动)/ NodeEditDrawer / NodeCreatorMenu / RelationshipCreator / ReshapeSlider / CounterfactualWorkbench / GraphHelpModal / ProjectUploadsPanel / CanonicalGuardianPanel / ExportSimulationDialog / ConfirmDialog / ToastHost / SceneGraph / ProtagonistWall / RelationshipTimeline / BehaviorBaselineEditor / WorldBaselineEditor / EntityReviewModal / CreateComicModal / RefImageUploadDialog / StyleVotingDialog / BadgeDownloadDialog / Icon / SimulationsListPanel / SkeletonBlock / CreditEstimate
        ├── types/                        graph.ts(CrystalGraph 内部类型)+ refinement.ts(对焦类型)
        ├── views/                        10 个:DashboardView / ProjectView / ProjectGraphView / SimulationDetailView / OutlineReviewView / MyComicsView / ComicProjectView / ComicReaderView / CounterfactualTreeView / CreditTransactionsView
        └── styles/tokens.css + global.css 浅色 token 系统(8 级 z-index 分层 modal/nested/confirm/toast/hover)
```

---

## 创始人初心

项目最大资产是**情感连接**。每周写一次私密初心日志,只回答:"这一周做的事,和最初那个意难平相关吗?"

doc 5 质量基准:

> 你这个项目最大的资产是你的情感连接,最大的风险是这份情感连接被各种"现实考虑"稀释到失真。
> 当你在某个深夜对着代码或会议纪要疲惫不堪时,记得回头看一眼最初让你想做这件事的那个意难平 — 它不是你这个产品的"灵感来源",它是你这个产品的"质量基准"。

---

## 文档索引

**规划档案库**(只读,只看不改):`C:\Users\Administrator\Desktop\项目文件\`
- **2** 我对产品的初步描述 ✅ — 三态原始定义 / 蛛网视觉语言 / 重塑度阈值起源 / **4 态心智边界源头**(执行纪律 6 必读)
- **3** Claude 对产品深度解析 ✅ — 救赎工具底层定位 / 北极星 / vs MiroFish 范式差异 / 5 大致命风险 / 5 大具体建议 / 正典守护者机制
- **5** Claude 对疑问的深度解答 ✅ — 工具能力定价模型 / 四层版权防护 / 角色对焦机制 / 同人圈三策略 / 投资三路径 / **反向提醒**(质量基准源头)
- 其他规划档(1/4/6/7/8/10/11/13)按需调阅,与日常开发非强相关

**工程产物**:`C:\Users\Administrator\Desktop\huimeng\`(本仓库)
- `docs/后端架构ADR_v1.md` / `docs/MVP阶段1_角色对焦组件设计.md` / `docs/pitch_deck_v2.md` / `docs/律师咨询问题清单.md` / `docs/法务草稿调整说明.md`
- **`docs/ADR_漫画创作态架构.md`**(v3,2026-05-12):第 4 态重定义为漫画创作态的架构愿景。**v3 修订**:10 agent / 4 层角色一致性 / AI 对焦扩展到 5 类素材 / 用户上传 3 张参考图 + Qwen-VL 视觉 DNA / 单本成本 ¥53.6 / 14-16 sprint — 开发漫画态前**必读 + 评审更新**
- **`docs/ADR_D.8_国产API路由层.md`**(2026-05-12,**已 sign-off + 完工**):Adapter Pattern + Protocol;主路由锁定 DeepSeek V3 / Doubao Seedream 3.0 / Qwen-VL Max
- **`docs/ADR_credit_quota_重构.md`**:credit 单位制 + 4 档 + 加购包 + 阶梯优惠
- **`docs/D.8_vendor_evaluation.md`**(2026-05-12):D.8 横评报告
- **`docs/history/项目记忆_v15.1_full_snapshot.md`**:本工作记忆裁剪前的 3957 行完整原版,所有已完工 sprint 实施细节、bug 修复历程、测试矩阵在此查
